1. 上午上课，赶上杨老师测试，就跟着试了试，但是太久不碰汇编，直接看汇编代码有不少指令都不明白了。

   下午在写论文，写了方法里自协同学习的部分，后面应该还要再扩充，加入符号。

   晚上看了下新发表的差分隐私的文章，没有需要关注的。
   然后在做逆向的题目，x64的题目，要求通过软件的激活码，里面三个校验函数，大致看了前两个，第二个比较复杂，是一个链式的校验计算，
   前面的值影响后面的值，最后校验几个数，之后准备细看一下逻辑。发现对C的指针掌握太差了，之后需要补课。