构思了一下论文的contribution，主要是4点：
1. 手机或车辆的终端不断移动，难以维持长时间的通信 ———— 需要减少训练的总时间，而不只是通信频率
2. 通常参与者已完成本地模型的训练，但不同参与者的模型结构不同，传统的聚合方法不适用 ———— 聚合输出而不是模型参数，规避模型异构问题的同时减少了通信开销
3. 利用本地差分隐私实现隐私保护 ———— 对于模型输出y，选择扰动argmax(y)而不是直接扰动y，实现了降维效果，提高了数据效用的
4. 通过匿名通信实现shuffle model，实现了隐私放大的效果，减少了privacy loss


然后一直在跑实验，着重测试了下我的方法中聚合后标记的精度问题，因为同时涉及到预测错误带来的误差和差分隐私带来的误差：
在实验设置上，cifar10数据集下，100个参与者，每个参与者随机拥有6种标签的数据来模拟non-iid设置(不知道这样模拟是否可行)

实验1：加入差分隐私保护，每个输出的隐私预算为2，公共数据集含有10000个样本，100个参与者的输出聚合后得到的伪标签准确度为63% (6288/10000)，
      训练100轮得到的全局模型精度为57.8%
实验2：无隐私保护设置，其余参数同上，聚合后伪标签精度为 59% (5901/10000)，全局模型精度为52%


实验2和1还有些小细节不同，主要是验证另一个思路，但大体可以看出差分隐私没有额外降低精度，这个idea是可以work的，只是隐私损失有点大，每个用户的损失为2*10000
接下来准备加入shuffle model降低隐私损失，不过估计能降低的有限，大概能降低50%左右，看起来幅度很大但还是挺高的


接下来先把理论论证搞清楚，搞清楚shuffle model怎么用
