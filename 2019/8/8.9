1. 在将个人数据原则PDP应用于差分隐私时有以下需要注意的地方：
（1）对于邻近数据集的定义是否正确？
     对于图数据而言，以某条边有无为区别的edge-DP和以某个节点的有无为区别的node-DP，
     前者对邻近数据集的定义是存在问题的，因为当每个节点代表一个个体的话，删去一条边
     并不等同于控制个体数据，同时对匿名图数据的攻击也是以重新识别节点的形式进行的。
（2）在使用本地设置时，关键在于需要系统收集答案的问题有多少个？以及如何选择隐私预算ε？
     因为系统会根据收集到数据回答大量的问题，当这些问题之间存在关联的时候，很难确定到
     底实现了什么程度的隐私保护，因为问题间关联性可能使得敌手获得更精确的答案，从而导
     致隐私保护的失效。而隐私预算ε则是对每个问题可以获得回答的精确程度的限制，结合上述
     关联性的问题也使得该数的确定需要被慎重考虑。
（3）应用个人隐私原则PDP时首先需要确定个体的私人数据是什么？
     例如对于基因和健康数据而言，其具有很强的关联性，父母的基因片段也可以被认为是子女
     的一部分，其次还有与其他亲属的关联。所以在处理类似的数据时，不能因为个人数据原则
     而简单地说相关性无关紧要，而可能要对这种相关性进行量化和处理。
	 
2. 学习了在实际应用机器学习时，应该如何优化算法，提高性能的方法。首先是建立假设评估的函数，
   用来评价假设函数的性能；其次将数据集进行划分按照6:2:2的比例划分为训练集、验证集，测试集，
   其中验证集用来验证假设函数对额外数据的拟合程度，测试集用来测试算法误差；之后通过建立不同
   的图像，如多项式项数与假设函数性能的关系，正则化项参数与假设函数性能的关系，数据集大小与
   假设函数性能的关系等进行判断，得出结果如下：
（1）当存在高偏差问题(欠拟合)时，可以采取增加特征值数量、增加多项式项数，减小正则化项参数的办法；
（2）当存在高方差问题(过拟合)时，可以采取减少特征值数量、减小数据集大小，增大正则化项参数的办法。