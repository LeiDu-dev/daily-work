1. 继续学习在机器学习中应用差分隐私，今天学习的是对现有算法的改进，即采用非隐私保护的机器学习算法，
   并对其应用拉普拉斯机制或指数机制来确保其每一步都是私密的。通常会采用迭代算法，然后使每次迭代私密化。
   文中主要介绍了两个具体的应用：
    1) DPLLOYD：
       该方法应用于k-means聚类算法中，简单来说就是在计算当前划分簇数与计算簇内数据点坐标和时加入拉普拉斯噪声，
       由此计算得来的新均值向量就是含噪的。在运用该算法时需要决定如何选择初始值(k-means算法中初始划分)、如何
       选择迭代次数(迭代次数太大会添加过多噪声，太小会难以聚合)，以及如何在每轮迭代中划分隐私预算(默认等分)。
    2) DIFFPID3：
       该方法应用于ID3决策树，使其满足差分隐私。但这个算法之前学机器学习没遇到所以没看懂，之后学了再细看。
